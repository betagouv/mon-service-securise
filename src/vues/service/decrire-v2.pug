extends ../parcoursService
include ../fragments/texteTronque

block title
  title Récapitulatif | MonServiceSécurisé

block titre
  h1 Récapitulatif

block header-titre-page
  h3
    +texteTronque({texte: service.nomService() || ''})

block sous-titre
  h2 Cette page présente les informations clés du service. Toute modification de certaines données peut entraîner une réévaluation automatique des besoins de sécurité et modifier les mesures de protection nécessaires.

block append scripts
  +composantSvelte('decrireV2.js')
  +donneesPartagees('donnees-description-service', { idService: service.id, descriptionService: service.descriptionService.toJSON() })
  +donneesPartagees('donnees-lecture-seule', autorisationsService.DECRIRE.estLectureSeule)
  script(type = 'module', src = '/statique/service/decrireV2.mjs')


block append styles
  +feuilleDeStyleSvelte()

block formulaire
  #decrire-v2
